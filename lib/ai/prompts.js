export const SYSTEM_PROMPT = `Ты — редактор биографического портала «Личности».
Портал посвящён ИСКЛЮЧИТЕЛЬНО российским, советским и русскоязычным публичным персонам:
граждане и выходцы России, СССР, РСФСР, СНГ, Российской империи; деятели русской культуры.
Иностранные персоны (западные, азиатские и т.п.) на портале НЕ публикуются.

Принципы:
- Пиши фактологично, нейтрально, на русском языке, без домыслов.
- Если факт не подтверждён — не выдумывай, укажи "нет достоверных данных".
- Используй HTML-разметку только из тегов: p, h2, h3, ul, ol, li, strong, em, blockquote, a.
- Если запрошенная персона не подпадает под критерии русской/советской/русскоязычной публичной фигуры,
  верни строго JSON {"error":"not_russian","reason":"…"} и больше ничего.`;

export const PROMPTS = {
  validate_russian: ({ name }) => ({
    system: SYSTEM_PROMPT,
    user: `Проверь, подходит ли персона "${name}" для русскоязычного биографического портала.
Критерии "да":
- гражданин/уроженец/выходец России, СССР, РСФСР, Российской империи, СНГ;
- деятель русской/советской культуры (литература, кино, музыка, спорт, наука, политика);
- этнический русский, работающий/работавший преимущественно в русскоязычном культурном пространстве.

Верни СТРОГО JSON без пояснений:
{
  "is_russian": true|false,
  "category_hint": "znamenitosti|obrazovanie|sport|gosudarstvo|biznes|blogery|eksperty|other",
  "reason": "одно предложение почему"
}

Подсказки по категориям:
- znamenitosti — актёры, музыканты, писатели, режиссёры, деятели культуры
- obrazovanie  — учёные, преподаватели, популяризаторы науки
- sport        — спортсмены, тренеры
- gosudarstvo  — политики, чиновники, исторические деятели власти
- biznes       — предприниматели, топ-менеджеры
- blogery      — блогеры, стримеры, ютуберы
- eksperty     — специалисты, эксперты публичной сферы`
  }),

  full_bio: ({ name, category, context }) => ({
    system: SYSTEM_PROMPT,
    user: `Напиши полную биографическую статью о российской/советской/русскоязычной персоне "${name}"${
      category ? ` (категория: ${category})` : ''
    }.
${context ? `Дополнительный контекст: ${context}\n` : ''}
ВАЖНО: если это не российская/советская/русскоязычная публичная фигура — верни только {"error":"not_russian"}.

Верни СТРОГО JSON (без markdown-блоков, без пояснений) следующей структуры:
{
  "profession": "кратко, 2-4 слова: 'Актёр, режиссёр' / 'Учёный-физик'",
  "intro": "одно предложение-затравка для карточки (что это за человек, 1-2 коротких предложения)",
  "sections": [
    {"anchor_id":"detstvo-semya","title":"Детство, семья","html":"<p>...</p><p>...</p>"},
    {"anchor_id":"obrazovanie-karera","title":"Образование и карьера","html":"<p>...</p>"},
    {"anchor_id":"lichnaya-zhizn","title":"Личная жизнь","html":"<p>...</p>"},
    {"anchor_id":"dostizheniya","title":"Достижения","html":"<ul><li>...</li></ul>"}
  ],
  "zodiac": "Стрелец или null",
  "birth_place": "город, регион или null",
  "birth_date": "ДД.ММ.ГГГГ или null",
  "death_date": "ДД.ММ.ГГГГ или null",
  "social_links": [{"url":"https://t.me/..."},{"url":"https://vk.com/..."}]
}

HTML-теги разрешены только: p, h3, ul, ol, li, strong, em, blockquote, a.
Объём каждой section.html — 2-6 абзацев. Всего 800–1500 слов.
Если фактов для раздела нет — сделай его короче, но не пропускай полностью.
Если факт не подтверждён — пиши "нет достоверных данных", не выдумывай.`
  }),

  seo_meta: ({ name, bio }) => ({
    system: SYSTEM_PROMPT,
    user: `На основе биографии ниже составь SEO-метаданные для страницы о "${name}".
Верни строго JSON без пояснений:
{
  "title": "не более 60 символов, включает имя и ключевое слово 'биография'",
  "description": "140–160 символов, естественный язык, с CTA 'Читать подробнее'",
  "keywords": "8–12 фраз через запятую, без дублей",
  "h1": "основной заголовок страницы, до 80 символов"
}

Биография:
${bio.slice(0, 4000)}`
  }),

  short_bio: ({ name, bio }) => ({
    system: SYSTEM_PROMPT,
    user: `Сократи биографию ниже до 2–3 предложений (до 280 символов) для карточки "${name}". Только текст, без HTML.

Биография:
${bio.slice(0, 3000)}`
  }),

  similar_persons: ({ name, profession, category }) => ({
    system: SYSTEM_PROMPT,
    user: `Назови 6 реально существующих российских/советских/русскоязычных публичных персон, наиболее похожих на "${name}"${
      profession ? ` (${profession})` : ''
    }${category ? ` из области "${category}"` : ''}.
ТОЛЬКО русские/советские/СНГ-персоны. Иностранцев не включать.
Верни строго JSON-массив объектов:
[{"name":"ФИО","reason":"почему похож, 1 фраза"}]`
  }),

  has_new_info: ({ name, currentBio, lastUpdate }) => ({
    system: SYSTEM_PROMPT,
    user: `Биография "${name}" последний раз обновлялась ${lastUpdate}.
Проанализируй её (ниже) и реши, появилась ли за это время значимая новая публичная информация (новые роли, награды, события, смерть, резонанс).
Верни строго JSON:
{"has_new": true|false, "new_facts": ["факт 1", "факт 2"]}

Текущая биография:
${currentBio.slice(0, 6000)}`
  }),

  fact_check: ({ name, bio }) => ({
    system: SYSTEM_PROMPT,
    user: `Проверь биографию "${name}" на фактические ошибки, противоречия, выдуманные данные.
Верни JSON:
{"ok": true|false, "issues": ["описание ошибки", ...]}

Биография:
${bio.slice(0, 6000)}`
  })
};

export function buildPrompt(key, vars) {
  const fn = PROMPTS[key];
  if (!fn) throw new Error(`Unknown prompt key: ${key}`);
  return fn(vars || {});
}
